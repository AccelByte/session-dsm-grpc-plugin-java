/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java application project to get you started.
 * For more details take a look at the 'Building Java & JVM projects' chapter in the Gradle
 * User Manual available at https://docs.gradle.org/7.5/userguide/building_java_projects.html
 */

plugins {
    // Apply the application plugin to add support for building a CLI application in Java.
    id 'java'
    id 'application'
    id 'io.freefair.lombok' version '6.4.2'
    id 'com.palantir.graal' version '0.12.0'
}

group = 'net.accelbyte'
version = '0.0.1'
sourceCompatibility = '11'
project.buildDir = 'target'


repositories {
    // Use Maven Central for resolving dependencies.
    mavenCentral()
}

dependencies {
    // Use JUnit Jupiter for testing.
    testImplementation 'org.junit.jupiter:junit-jupiter:5.8.2'

    annotationProcessor 'org.projectlombok:lombok:1.18.24'

    // These dependencies are used by the application.
    implementation 'info.picocli:picocli:4.7.1'
    implementation 'com.google.guava:guava:31.0.1-jre'
    implementation 'org.projectlombok:lombok:1.18.24'
    implementation 'net.accelbyte.sdk:sdk:0.69.0'
    implementation 'com.squareup.okhttp3:okhttp:4.9.1'

    // this dependency is used to remove annoying "warning: unknown enum constant Include.NON_NULL reason: class file for com.fasterxml.jackson.annotation.JsonInclude$Include not found"
    // but this can safely removed since the app itself didn't used it.
    implementation 'com.fasterxml.jackson.core:jackson-annotations:2.13.0'
}

application {
    // Define the main class for the application.
    mainClass = 'net.accelbyte.extend.session.demo.App'
}

graal {
    javaVersion '11'
    graalVersion '22.3.1'
    mainClass 'net.accelbyte.extend.session.demo.App'
    outputName 'sessionDsmGrpcDemo'
}

tasks.named('test') {
    // Use JUnit Platform for unit tests.
    useJUnitPlatform()
}